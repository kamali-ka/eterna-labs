# Eterna Pulse — Scaffold

This repo contains a minimal scaffold for building a pixel-perfect token discovery table inspired by Axiom Trade.

To get started:

```bash
npm install
npm run dev
```

Next steps:
- Implement `app/pulse` feature route
- Add table components under `components/` using atomic architecture
- Wire WebSocket mock in `services/mockSocket.ts` and use Redux/React Query as needed
